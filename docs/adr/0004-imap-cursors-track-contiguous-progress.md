# IMAP 游标表示连续检查进度

同步游标只推进到已经连续完成检查的最高 UID；积压超过批量上限时从最旧的未检查邮件开始处理，不得用搜索结果的最大 UID 跳过未读取邮件。游标同时绑定 `UIDVALIDITY`；邮箱世代变化时清除旧游标并按现有 lookback 配置回看最近 24 小时，通过远端消息标识去重。

## Consequences

- FETCH 整体失败时不推进游标；成功读取但不相关或无法解析的单封邮件记录原因后可视为已检查，避免坏邮件永久阻塞队列。
- 同步必须分批提交消息和对应的连续游标，进程中断后允许重复检查，不允许形成缺口。
