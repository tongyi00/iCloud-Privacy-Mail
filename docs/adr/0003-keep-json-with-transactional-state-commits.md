# 保留 JSON 并采用事务式状态提交

本轮修复继续使用单文件 JSON，不迁移 SQLite；所有状态变更先作用于副本，完整写入临时文件并原子替换成功后，才发布为进程内状态，同时将一次邮件同步中的消息与游标合并提交。当前约 3.54 MiB 状态仍可由该方案承载；只有在批量提交后实测写入延迟或文件规模仍不可接受时，才启动数据库迁移。

## Consequences

- 落盘失败不会污染内存状态，也不会在后续成功保存时意外带入失败变更。
- 密码哈希和外部网络请求不得在状态提交锁内执行。
- JSON 仍是单进程存储，不支持多个服务实例共享写入。
