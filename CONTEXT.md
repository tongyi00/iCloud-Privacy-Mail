# iCloud 隐私邮箱取码

本上下文描述隐私邮箱同步、验证码查询及 Apple 登录归属的统一业务语言。

## Language

**隐私邮箱**：
一个可接收邮件并通过独立访问凭据查询验证码的 iCloud 隐藏邮箱地址。
_Avoid_: 临时邮箱、一次性邮箱

**有效验证码**：
仍处于新鲜时间窗口内、且符合查询关键词的最新验证码。有效验证码可被重复读取，不会因一次查询而消费。
_Avoid_: 未消费验证码、可领取验证码

**验证码查询**：
对有效验证码的幂等读取；同一验证码在有效期内可以返回给多个授权请求。
_Avoid_: 验证码领取、验证码发放、验证码消费

**面板验证码查询**：
面板用户在当前登录会话中读取自己可访问邮箱的有效验证码。
_Avoid_: 公开 API 调用、token URL 调用

**外部验证码 API**：
面板之外的客户端使用邮箱独立访问凭据读取有效验证码的接口。
_Avoid_: 面板取码、Web Session 取码

**邮箱归属**：
邮件依据结构化收件人地址关联到唯一隐私邮箱，主题、发件人和扩展邮件头不构成归属证据。
_Avoid_: 邮件头命中、别名子串命中

**同步游标**：
同一邮箱世代内，所有不大于该位置的邮件均已完成检查的连续进度边界；它不是“最近看过的最大编号”。
_Avoid_: 最新 UID、最大 UID

**面板用户**：
登录本系统并拥有账号、隐私邮箱与 Apple 登录流程的租户主体。
_Avoid_: Apple 账号、邮箱账号

**管理员初始化**：
空系统使用一次性启动凭据创建首位管理员的过程；完成后不可再次公开执行。
_Avoid_: 首个注册者自动成为管理员、公开抢注

**用户开通**：
管理员在已认证会话中创建普通面板用户的受控操作。
_Avoid_: 匿名注册、公开注册

**用户关闭**：
先阻止面板用户的新操作并等待其后台工作停止，随后删除其本地数据的管理过程；未完成停止时用户仍然存在。
_Avoid_: 强制删除、先删后停

**Apple 登录流程**：
由一个面板用户发起并独占的、从凭据校验到二次验证完成的登录过程。
_Avoid_: 全局登录、共享 pending
